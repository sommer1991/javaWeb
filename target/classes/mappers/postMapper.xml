<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weibo.dashboard.dao.PostMapper">
	<resultMap id="postMap" type="com.weibo.dashboard.entity.Post">
		<id property="id" column="id" javaType="int"/>
		<result property="date" column="date" javaType="Date"/>
		<result property="authorId" column="author_id" javaType="int"/>
		<result property="content" column="content" javaType="String"/>
		<result property="likes" column="likes" javaType="int"/>
		<collection property="commentList" column="commentList" ofType="com.weibo.dashboard.entity.Comment">
			<id property="id" column="id" javaType="int"/>
			<result property="date" column="date" javaType="Date"/>
			<result property="content" column="content" javaType="String"/>
			<result property="authorId" column="author_id" javaType="String"/>
		</collection>
	</resultMap>
	<select id="findList" resultMap="postMap">
		select * from posts,comments where post_id = posts.id
	</select>
	<select id="postByUser" resultMap="postMap">
		select * from posts,comments where post_id = posts.id and posts.author_id = #(userId)
	</select>
	<insert id="insert" parameterType="com.weibo.dashboard.entity.Post">
		insert into posts
		(date,author_id,content)values
		(now(),#authorId,#content))
	</insert>
	<delete id="delete">
		delete from posts where id=#{id}
	</delete>
	<update id="like">
		update posts set likes=likes+1 where id=#{id}
	</update>
	<update id="disLike">
		update posts set likes=likes-1 where id=#{id}
	</update>
</mapper>